import { Route } from '@angular/router';
import { FeatureCartComponent } from './feature-cart/feature-cart.component';

export const featureCartRoutes: Route[] = [
  { path: '', component: FeatureCartComponent },
];
